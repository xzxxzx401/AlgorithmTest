#include"main.h"

int main()
{

	#pragma region 二分搜索
	/*int n;scanf("%d", &n);
	int i;int a[100];
	for(i=0;i<n;i++)
	{
		scanf("%d", &(a[i]));
	}
	int res=BinarySearch(a, n, 4);
	printf("%d\n", res);*/
	#pragma endregion

	#pragma region 归并排序
	/*int a[100], b[100];
	int n;scanf("%d", &n);
	for(int i=0;i<n;i++)
	{
		scanf("%d", &(a[i]));
	}

	MergeSort(a, 0, n-1, b);
	for(int i=0;i<n;i++)
	{
		printf("%d ", a[i]);
	}*/
	#pragma endregion

	#pragma region 快速排序
	/*int a[100];
	int n;scanf("%d", &n);
	for(int i=0;i<n;i++)
		scanf("%d", &(a[i]));
	Qsort(a, 0, n-1);
	for(int i=0;i<n;i++)
		printf("%d ", a[i]);*/
	#pragma endregion




	system("pause");
	return 0;
}

